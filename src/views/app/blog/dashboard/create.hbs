<div class="Create">
    <form action="{{ basePath }}/dashboard/blog/create" method="post">
        <h2>{{{ icon "plus" }}} {{ section }}</h2>

        {{#if message }}
            <h3>{{{ message }}}</h3>
        {{/if}}

        {{{
            renderSchema
                schema = schema
                flashData = flashData
                userInfo = userInfo
                __ = __
                securityToken = securityToken
        }}}
    </form>
</div>
